const test = QUnit.test;
import win from '../src/game/calculations/win.js';
test('when given a winning scoreboard', function(assert) {
    const result;
    const expected;
    assert.equal(result, expected);
});